name = "filme-de-hoje"
main = "src/index.js"
compatibility_date = "2026-08-27"

# 12:00 UTC = 9h em Recife (mesmo horário do workflow do GitHub Actions)
[triggers]
crons = ["0 12 * * *"]

# BOT_TOKEN, TMDB_KEY e CHAT_ID são secrets: npx wrangler secret put <NOME>
# Localmente, ficam em .dev.vars (fora do git).
